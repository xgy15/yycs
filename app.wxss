/* 全局引入图标 */
@import './styles/iconfont.wxss';

page,view,text,image,button,input,swiper,swiper-item,navigator,checkbox,rich-text,radio{
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

image{
  width: 100%;
}

/* 使用 css 原生的变量，设置一个主题色 
  使用的话：var(--themeColor) */
page {
  --themeColor: #eb4450
}